import React from "react";
import SyncButton from "./sync-button";

const SyncContainer = () => {
  return (
    <div className="sync-box">
      <SyncButton />

      <div className="title">Sync Contacts</div>
    </div>
  );
};

export default SyncContainer;
